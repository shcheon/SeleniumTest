class InitUserDeviceVO:
    def __init__(self, userId='', userPassword='', resetUserId=''):
        self.userId = userId
        self.userPassword = userPassword
        self.resetUserId = resetUserId
